# Порт для gRPC-сервера
GRPC_HOST_PORT=localhost:50051

# Порт для HTTP-сервера
HTTP_PORT=8081
# Значение для переменной окружения DATABASE_MASTER_HOST_PORT будет динамически вычисляться в тестах на основе открытого порта в контейнере
DATABASE_MASTER_USER=testuser
DATABASE_MASTER_PASSWORD=testpassword
DATABASE_MASTER_NAME=testdb
