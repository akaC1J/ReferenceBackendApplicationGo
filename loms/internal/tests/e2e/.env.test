# Порт для gRPC-сервера
GRPC_HOST_PORT=localhost:50051

# Порт для HTTP-сервера
HTTP_PORT=8081
# Значение для переменной окружения DATABASE_MASTER_HOST_PORT_0 будет динамически вычисляться в тестах на основе открытого порта в контейнере
DATABASE_MASTER_USER_0=testuser
DATABASE_MASTER_PASSWORD_0=testpassword
DATABASE_MASTER_NAME_0=testdb
